# 🎯 区域搜索航线优化方案

## 📋 问题诊断

**当前问题：**
- ❌ 航线过于直线，缺乏搜索行为
- ❌ 没有在目标区域聚集和密集搜索
- ❌ 避障行为不明显
- ❌ 多机协同不明显，路径相似

**根本原因：**
1. **动作空间太小**：6个动作（上下左右前后）无法实现曲折路径
2. **步长太大**：spacing=5，步伐过大导致粗糙路径
3. **覆盖奖励太弱**：coverage_weight=0.15，无法驱动密集搜索
4. **区域奖励不突出**：region_priority_weight=5.0，不够强调优先级
5. **足迹惩罚过强**：footprint_weight=0.5，阻止重复搜索重点区域
6. **探索不足**：eps_max=0.5，探索度不够

---

## ✨ 核心优化策略

### 1. **动作空间升级：6 → 27** 🚀
```yaml
num_actions: 27  # 从 6 改为 27
```
**效果：**
- 3D立体搜索能力（26个方向 + 1个停留）
- 可以实现对角线移动、螺旋搜索、之字形路径
- 更灵活的障碍物规避策略

### 2. **步长优化：5m → 3m** 📏
```yaml
spacing: 3  # 从 5 改为 3
```
**效果：**
- 路径更密集、更细腻
- 可以实现曲折的搜索模式
- 更精细的区域覆盖

### 3. **航程延长：35步 → 50步** ⏱️
```yaml
budget: 50  # 从 35 改为 50
```
**效果：**
- 有足够的步数进行充分搜索
- 可以完成复杂的搜索路径
- 覆盖更多区域

### 4. **覆盖奖励大幅提升** 📊
```yaml
coverage_weight: 3.0        # 从 0.15 提升到 3.0 (20倍)
footprint_weight: 0.0       # 从 0.5 降低到 0.0
coverage_exploration_weight: 4.0  # 从 2.0 提升到 4.0
```
**效果：**
- 强烈驱动agent覆盖更多区域
- 允许重复搜索高优先级区域（降低足迹惩罚）
- 密集搜索模式

### 5. **区域优先级强化** 🎯
```yaml
region_coverage_weight: 10.0   # 从 5.0 提升到 10.0
region_priority_weight: 10.0   # 从 5.0 提升到 10.0
search_density_weight: 5.0     # 从 2.0 提升到 5.0
search_completion_weight: 8.0  # 从 3.0 提升到 8.0
redundant_search_penalty: -0.05  # 从 -0.2 降低到 -0.05
```
**效果：**
- 明确驱动agent优先搜索高优先级区域
- 在重点区域聚集和密集搜索
- 完成区域搜索任务

### 6. **前沿探测驱动** 🔍
```yaml
frontier_reward_weight: 5.0  # 从 2.5 提升到 5.0
```
**效果：**
- 强烈驱动探索未知边界
- 产生向外扩展的搜索行为
- 避免原地打转

### 7. **协同机制强化** 🤝
```yaml
overlap_penalty_weight: 2.0      # 从 1.2 提升到 2.0
division_reward_weight: 2.0      # 从 0.6 提升到 2.0
region_assignment_bonus: 2.0     # 从 0.8 提升到 2.0
joint_discovery_weight: 3.0      # 从 1.5 提升到 3.0
```
**效果：**
- 避免多机路径重叠
- 鼓励分区搜索
- 协同覆盖不同区域

### 8. **避障优化** 🚧
```yaml
safety_margin: 1.2              # 从 0.5 提升到 1.2
collision_penalty: 8.0          # 从 5.0 提升到 8.0
obstacle_penalty_weight: 0.5    # 从 0.1 提升到 0.5
```
**效果：**
- 更安全的避障行为
- 路径会绕开障碍物
- 产生曲折的避障轨迹

### 9. **高度多样性** 📈
```yaml
altitude_diversity_weight: 2.0  # 从 0.5 提升到 2.0
```
**效果：**
- 鼓励在不同高度探索
- 3D立体搜索模式
- 利用高度优势

### 10. **探索率提升** 🎲
```yaml
eps_max: 0.7        # 从 0.5 提升到 0.7
eps_min: 0.05       # 从 0.02 提升到 0.05
eps_anneal_phase: 12000  # 从 10000 延长到 12000
```
**效果：**
- 更多随机探索，发现更好策略
- 避免过早收敛到次优解
- 持续探索新的搜索模式

### 11. **网络容量提升** 🧠
```yaml
actor.hidden_dim: 256      # 从 128 提升到 256
critic.fc1_dim: 128        # 从 64 提升到 128
batch_size: 128            # 从 64 提升到 128
data_passes: 5             # 从 3 提升到 5
```
**效果：**
- 学习更复杂的搜索策略
- 更好地处理27动作空间
- 更稳定的训练

---

## 🎯 预期效果

### 航线特征：
1. **曲折蜿蜒**：利用27动作空间，路径不再直线
2. **区域聚集**：在高优先级区域（10,10)-(25,25）密集搜索
3. **绕开障碍物**：遇到障碍物会改变方向，形成绕行路径
4. **多机协同**：4架UAV分散搜索不同区域，避免重叠
5. **3D立体**：利用不同高度进行搜索
6. **前沿驱动**：不断向未探索区域扩展

### 搜索行为：
- **阶段1（0-15步）**：从起点出发，快速移动到高优先级区域
- **阶段2（15-35步）**：在高优先级区域密集搜索，形成网格或螺旋模式
- **阶段3（35-45步）**：转向中优先级区域，继续搜索
- **阶段4（45-50步）**：补充低优先级区域

### TensorBoard指标变化：
- `RegionSearch/Coverage_Reward` ↑↑↑ 显著上升
- `RegionSearch/Priority_Reward` ↑↑ 明显上升
- `Coordination/Division_Reward` ↑ 出现非零值
- `Penalties/Collision` ↓ 降低（更好避障）
- `Frontier/Current_Points` 稳定在合理范围
- `trainRewards/Episode/mean` ↑ 平均奖励提升

---

## 📊 配置对比表

| 参数 | 原配置 | 新配置 | 改进幅度 | 目的 |
|------|--------|--------|----------|------|
| **动作空间** | 6 | 27 | +350% | 3D立体搜索 |
| **步长** | 5m | 3m | -40% | 路径密集 |
| **航程** | 35步 | 50步 | +43% | 充分搜索 |
| **覆盖奖励** | 0.15 | 3.0 | +1900% | 密集覆盖 |
| **区域覆盖奖励** | 5.0 | 10.0 | +100% | 区域聚集 |
| **区域优先级奖励** | 5.0 | 10.0 | +100% | 优先搜索 |
| **前沿探测奖励** | 2.5 | 5.0 | +100% | 边界探索 |
| **足迹惩罚** | 0.5 | 0.0 | -100% | 允许重复 |
| **安全边界** | 0.5m | 1.2m | +140% | 安全避障 |
| **探索率** | 0.5 | 0.7 | +40% | 充分探索 |

---

## 🚀 使用方法

### 1. **开始新训练**
```powershell
cd E:\code\paper_code\paper\running
.\start_training.ps1
```

### 2. **监控训练过程**
```powershell
# 在新终端中启动TensorBoard
cd E:\code\paper_code\paper\marl_framework
tensorboard --logdir=log
```
浏览器打开：http://localhost:6006

### 3. **观察关键指标**

**区域搜索指标（核心）：**
- `RegionSearch/Coverage_Reward`：区域覆盖奖励，应逐步上升
- `RegionSearch/Priority_Reward`：优先级奖励，应保持较高值
- `RegionSearch/Density_Reward`：搜索密度奖励，应上升
- `RegionSearch/Completion_Percentage`：完成度，应接近100%

**协同指标：**
- `Coordination/Division_Reward`：分工奖励，应出现非零正值
- `Coordination/Overlap_Penalty`：重叠惩罚，应接近0

**前沿探测：**
- `Frontier/Avg_Reward`：前沿奖励，应为正值
- `Frontier/Current_Points`：当前前沿点数，应合理波动

**避障指标：**
- `Penalties/Collision`：碰撞惩罚，应逐渐降低
- `Penalties/Distance`：距离惩罚，应较小

### 4. **评估航线质量**

观察生成的轨迹图（`marl_framework/res/`目录）：
- ✅ 路径是否曲折蜿蜒（不是直线）
- ✅ 是否在高优先级区域聚集
- ✅ 是否绕开了障碍物
- ✅ 多机是否分散在不同区域
- ✅ 是否覆盖了大部分搜索区域

---

## ⏱️ 训练时间估算

**配置变化影响：**
- Episodes: 1000 → 1500 (+50%)
- Budget: 35 → 50 (+43%)
- Actions: 6 → 27 (+350% 计算复杂度)
- Batch size: 64 → 128 (+100%)
- Data passes: 3 → 5 (+67%)

**预计训练时间：**
- 原配置：24-36小时
- **新配置：48-72小时**（建议周末开始训练）

**GPU利用率：**
- 预计：40-60%（增大batch size后GPU利用率提升）

---

## 🔧 快速调试技巧

### 如果训练太慢：
```yaml
# 减少episodes进行测试
n_episodes: 300  # 快速验证配置

# 减少budget进行测试
budget: 30  # 快速测试路径模式
```

### 如果路径还是太直：
```yaml
# 进一步降低步长
spacing: 2  # 更密集的路径

# 进一步提高覆盖奖励
coverage_weight: 5.0  # 更强的覆盖驱动
```

### 如果不避障：
```yaml
# 提高避障惩罚
collision_penalty: 15.0
obstacle_penalty_weight: 1.0
safety_margin: 2.0
```

### 如果不聚集在目标区域：
```yaml
# 进一步提高区域奖励
region_coverage_weight: 15.0
region_priority_weight: 15.0
```

---

## 📝 重要提示

1. **保存原配置**：原配置已经修改，如需回退可查看git历史
2. **监控训练**：定期查看TensorBoard，确保指标正常
3. **耐心等待**：27动作空间需要更多训练时间才能收敛
4. **逐步调整**：如果效果不理想，可以逐步微调参数
5. **保存模型**：训练过程中会自动保存最佳模型

---

## 🎓 理论依据

### 为什么这些改变会生成曲折航线？

1. **动作空间多样性**：27个动作提供了丰富的移动选项，可以实现对角线、螺旋等复杂模式

2. **奖励函数塑形**：
   - 高覆盖奖励 → 驱动访问更多网格
   - 低足迹惩罚 → 允许重复访问重点区域
   - 高区域奖励 → 吸引agent停留在目标区域
   - 高前沿奖励 → 驱动向边界扩展

3. **探索-利用平衡**：
   - 高探索率（0.7）→ 尝试新路径
   - 长衰减期（12000）→ 持续探索
   - 最小探索率（0.05）→ 保持一定随机性

4. **协同分工**：
   - 重叠惩罚 → 避免相同路径
   - 分工奖励 → 鼓励分散搜索
   - 每个agent会寻找不同的搜索模式

5. **避障驱动**：
   - 碰撞惩罚 → 强制改变方向
   - 安全边界 → 提前绕行
   - 产生曲折的避障轨迹

---

## 📞 需要帮助？

如果遇到问题：
1. 检查日志文件：`marl_framework/log/log_*.log`
2. 查看TensorBoard指标趋势
3. 检查GPU使用情况：`nvidia-smi`
4. 查看生成的轨迹图

预期成功标志：
- ✅ 训练正常运行，无错误
- ✅ 奖励逐步上升
- ✅ 生成的轨迹曲折且聚集在目标区域
- ✅ 成功避开障碍物
- ✅ 多机协同明显
